---
import Layout from "@/layouts/Layout.astro";
import "@/styles/globals.css";
import type { User } from "@/types";
const { userData }: { userData: User } = Astro.locals;
---

<Layout title="Lyric Kindle">
  <p>Private Route</p>
  <p>Hi, {userData.info?.full_name}</p>
</Layout>
